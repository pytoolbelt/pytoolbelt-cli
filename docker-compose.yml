x-base-service: &base-service
  image: thekevjames/nox
  volumes:
    - .:/app
  working_dir: /app

services:
  nox-test-all:
    <<: *base-service
    command: nox

  nox-tests-3.10:
    <<: *base-service
    command: nox --session tests-3.10
